<%= form_with(model: product_specification, local: true) do |form| %>
  <% if product_specification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_specification.errors.count, "error") %> prohibited this product_specification from being saved:</h2>

      <ul>
      <% product_specification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value, id: :product_specification_value %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id, id: :product_specification_product_id %>
  </div>

  <div class="field">
    <%= form.label :specification_id %>
    <%= form.text_field :specification_id, id: :product_specification_specification_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
